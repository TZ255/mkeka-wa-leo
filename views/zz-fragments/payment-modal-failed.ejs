<% 
const message_failed = `Email: ${user?.email || ''}\n\nChangamoto ya malipo.`;
const whatsapp_failed = `https://wa.me/255711935460?text=${encodeURIComponent(message_failed)}`; 
%>

<!-- OOB update: failed state in the same modal -->
<h5 id="paymentModalTitle" hx-swap-oob="true" class="modal-title text-danger">
  <i class="fa-solid fa-circle-xmark me-1"></i> Muda Umekwisha
</h5>

<div id="paymentModalBody" hx-swap-oob="true" class="modal-body">
  <div class="alert alert-danger d-flex flex-column align-items-center mb-2" role="alert">
    <i class="fa-solid fa-triangle-exclamation"></i>
    <div>
      <p>Hatukupokea uthibitisho wa malipo kwa wakati, tafadhali <strong>Anza Upya</strong> ikiwa bado hujathibitisha au
        kupokea menu ya malipo</p>
      <p>
        Ikiwa tayari umethibitisha bonyeza <b>wasiliana nasi</b> WhatsApp kwa kututumia screenshot ya muamala wako
      </p>
    </div>
  </div>
  <div class="small text-muted">Order ID: <code><%= orderId %></code></div>
  <div class="small text-muted">WhatsApp: <code>+255 711 935 460</code></div>

</div>

<div id="paymentModalFooter" hx-swap-oob="true" class="modal-footer d-flex justify-content-between">
  <button type="button" class="btn btn-outline-secondary" onclick="window.location.reload()">
    <i class="fa-regular fa-circle-xmark me-1"></i>
    ANZA UPYA
  </button>
  <a class="btn btn-success d-inline-flex align-items-center" href="<%= whatsapp_failed %>" target="_blank">
    <i class="fa-brands fa-whatsapp me-2"></i>
    WASILIANA NASI
  </a>
</div>
<style>
    *,
    *::before,
    *::after {
        box-sizing: border-box;
    }

    .auth-shell {
        background:
            radial-gradient(circle at 15% 20%, rgba(255, 183, 0, 0.18), transparent 40%),
            radial-gradient(circle at 80% 10%, rgba(0, 150, 255, 0.12), transparent 38%),
            var(--primary-bg);
    }

    .login-form {
        width: 95% !important;
        max-width: 520px !important;
        margin: 0 auto !important;
        background-color: var(--secondary-bg);
        border: 1px solid rgba(255, 255, 255, 0.08);
        box-shadow: 0 24px 70px rgba(0, 0, 0, 0.45);
        backdrop-filter: blur(6px);
    }

    .login-form .hubColor {
        color: var(--accent);
    }

    .auth-pill {
        display: inline-flex;
        padding: 0.35rem 0.95rem;
        border-radius: 999px;
        background: rgba(255, 183, 0, 0.14);
        color: var(--accent);
        font-weight: 700;
        letter-spacing: .08em;
        font-size: 0.72rem;
    }

    .auth-subtitle {
        color: rgba(255, 255, 255, 0.78);
    }

    .google-btn {
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 0.75rem;
        padding: 0.95rem 1.25rem;
        border-radius: 0.95rem;
        font-weight: 700;
        font-size: 1.03rem;
        color: #1f1f1f;
        background: linear-gradient(180deg, #ffffff 0%, #f1f3f4 100%);
        border: 1px solid rgba(255, 255, 255, 0.85);
        box-shadow: 0 12px 30px rgba(0, 0, 0, 0.28), 0 0 0 4px rgba(255, 183, 0, 0.14);
        transition: transform .18s ease, box-shadow .18s ease;
        text-decoration: none;
    }

    .google-btn:hover {
        transform: translateY(-2px);
        box-shadow: 0 16px 34px rgba(0, 0, 0, 0.33), 0 0 0 4px rgba(255, 183, 0, 0.22);
        color: #111;
    }

    .google-icon {
        width: 34px;
        height: 34px;
        border-radius: 50%;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        background: #fff;
        color: #ea4335;
        box-shadow: inset 0 0 0 1px #e9ecef;
    }

    .auth-note {
        color: rgba(255, 255, 255, 0.65);
    }
</style>

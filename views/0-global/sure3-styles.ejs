<style>
/* Card shell */
#sure3_betslip_card {
  position: relative;
  max-width: 460px;
  margin: 24px auto;
  border: 1px solid #e9ecef;
  border-radius: 14px;
  overflow: hidden;
  background: #fff;
  box-shadow: 0 10px 30px rgba(0,0,0,0.08);
  font-family: system-ui, -apple-system, "Segoe UI", Roboto, Arial;
}

/* Visible header */
#sure3_betslip_card .betslip-header {
  background: linear-gradient(135deg, #2c3034, #2c3034);
  color: #fff;
  padding: 14px 16px;
  text-align: center;
  font-weight: 700;
  font-size: 1.1rem;
}

/* Matches container... only this region gets an overlay blur */
#sure3_betslip_card .matches-locked {
  position: relative;
}

/* Matches list */
#sure3_betslip_card .betslip-body {
  padding: 8px 4px;
}
#sure3_betslip_card .match {
  border: 1px solid #f1f3f5;
  border-radius: 12px;
  padding: 12px;
  margin-bottom: 12px;
  background: #fff;
}
#sure3_betslip_card .match .league {
  font-size: .8rem;
  color: #6c757d;
  display: inline-flex;
  align-items: center;
  gap: 6px;
}
#sure3_betslip_card .match .teams {
  font-weight: 600;
  margin-top: 4px;
}
#sure3_betslip_card .match .line {
  display: flex;
  justify-content: space-between;
  margin-top: 6px;
}
#sure3_betslip_card .pill {
  min-width: 84px;
  text-align: right;
  padding: 6px 10px;
  border-radius: 999px;
  background: #f8f9fa;
  border: 1px dashed #dee2e6;
  font-weight: 600;
}

/* Frosted blur overlay... approx "80%" readable */
#sure3_betslip_card .frost-lock {
  position: absolute;
  inset: 0;
  background: rgba(255,255,255,0.50);       /* opacity for 75% veil */
  backdrop-filter: blur(1px) saturate(1.05); /* soft blur so content is barely readable */
  -webkit-backdrop-filter: blur(1px) saturate(1.05);
  z-index: 5;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 16px;
  text-align: center;
}
#sure3_betslip_card .overlay-card {
  background: rgba(255,255,255,.9);
  border: 1px solid #e9ecef;
  border-radius: 16px;
  padding: 18px 20px;
  max-width: 320px;
  cursor: pointer;
}
#sure3_betslip_card .overlay-card .lock {
  width: 52px; height: 52px;
  display: inline-flex; align-items: center; justify-content: center;
  border-radius: 999px;
  background: #eef4ff; color: #daa520; font-size: 1.5rem;
  margin-bottom: 10px;
}
#sure3_betslip_card .overlay-card .heading { font-weight: 700; font-size: .98rem; }
#sure3_betslip_card .overlay-card .subtxt { font-size: .85rem; color: #6c757d; }
#sure3_betslip_card .overlay-card .btn { margin-top: 12px; }

/* Visible summary... no blur here */
#sure3_betslip_card .summary {
  background: #f8f9fa;
  border-top: 1px solid #e9ecef;
  padding: 14px 16px;
  font-size: .95rem;
}
#sure3_betslip_card .summary .row-line {
  display: flex;
  justify-content: space-between;
  padding: 3px 0;
}
#sure3_betslip_card .summary .value { font-weight: 500; }

/* Visible footer */
#sure3_betslip_card .betslip-footer {
  background: #f8f9fa;
  border-top: 1px solid #e9ecef;
  padding: 12px 16px;
  display: flex;
  justify-content: space-between;
  align-items: center;
}
#sure3_betslip_card .betslip-footer .btn {
  border-radius: 999px;
  font-weight: 700;
  padding: 8px 14px;
}
</style>